import { StyledSwitch } from './switch.styles';

const SwxSwitch = props => {
    return <StyledSwitch {...props} sx={{ ml: 1, width: '42px', height: '26px', padding: 0 }} />;
};

export default SwxSwitch;
